kind: Deployment
apiVersion: apps/v1
metadata:
  name: sample-app
  labels:
    app: dev-sample-app
spec:
  selector:
    matchLabels:
      deployment: dev-sample-app
  template:
    metadata:
      labels:
        deployment: dev-sample-app
        app: dev-sample-app
